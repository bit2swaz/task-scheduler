global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: scheduler
    static_configs:
      - targets:
          - scheduler-1:3000
          - scheduler-2:3000
          - scheduler-3:3000
