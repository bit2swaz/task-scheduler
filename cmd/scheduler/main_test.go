package main

import "testing"

// TestMain_Smoke verifies the binary compiles and the package is importable.
// It is intentionally trivial -- real tests live in their respective packages.
func TestMain_Smoke(t *testing.T) {
	t.Log("smoke test passed: binary compiles and package is healthy")
}
